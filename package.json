{
  "name": "n8n-on-render",
  "version": "1.0.0",
  "description": "n8n running on Render",
  "main": "index.js",
  "scripts": {
    "start": "n8n start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "n8n": "^1.39.0"  
    // Eu recomendo usar uma versão específica como "^1.39.0" (verifique a mais recente no npmjs.com/package/n8n)
    // ou apenas "^1" para pegar a última da série 1.x.x
    // O "x.x" no "^1.x.x" não é válido.
  },
  "engines": {
    "node": ">=18"
  }
}
